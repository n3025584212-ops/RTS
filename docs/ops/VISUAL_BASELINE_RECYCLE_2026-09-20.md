# FRONTLINE Visual Baseline Recycle Manifest — 2026-09-20

STATUS=EXECUTED
SOURCE=User directive 2026-09-20: RiverTownVisualSlice becomes the sole visual baseline; non-kind asset baselines go to the recycle bin.
SAFETY_TAG=`recycle/2026-09-20/pre-visual-baseline-rationalization` (points at `7032c91`, the last commit containing everything below)

## Classification rule

- SAME-KIND (kept, folded under the sole baseline): River Town high-fidelity family — scenes, scripts, assets/visual_slice, artifacts/visual_reset evidence.
- NON-KIND (recycled): box-proxy battlefield production line, Golden Scene visual spike (rejected in PR #34), quality/material test fixtures, and their asset library, evidence and CI workflows.

## Recycled from product/frontline-high-fidelity-slice-v1

| Category | Paths |
|---|---|
| Scenes (16) | `scenes/production/FullBattlefieldProductionV1.tscn`, `FullBattlefieldProductionV2.tscn`, `FullBattlefieldProductionV2LOD.tscn`, `FullBattlefieldProductionV2LOD_TownCandidate.tscn`, `FullBattlefieldProductionV8Candidate.tscn`, `FullBattlefieldProductionV9Candidate.tscn`, `FullBattlefieldProductionV10Candidate.tscn`, `FullBattlefieldProductionV11Candidate.tscn`, `FullBattlefieldProductionV12Candidate.tscn`, `FullBattlefieldTownProbeA.tscn`, `FullBattlefieldTownProbeB.tscn`, `FullBattlefieldTownProbeC.tscn`, `FullBattlefieldTownProbeD.tscn`, `GoldenSceneV1.tscn`, `QualityMaterialProofV1.tscn`, `QualityTestSceneV1.tscn` |
| Scripts (top-level) | `scripts/production/full_battlefield_production_v1.gd`, `full_battlefield_production_v2.gd`, `full_battlefield_production_v2_lod.gd`, `full_battlefield_production_v2_lod_v5.gd`, `full_battlefield_production_v2_lod_v7.gd` – `v12.gd`, `full_battlefield_town_probe_a–d.gd`, `golden_scene_v1.gd`, `battlefield_visual_budget.gd`, `asset_review.gd` (+ .uid) |
| Scripts (dirs) | `scripts/production/golden/` (17 scripts: golden_world_v1–v20, golden_units_v1, …), `scripts/production/quality/` (41 scripts: quality_test_world_v1–v16_x, quality_material_proof_v2–v9, quality_test_vfx_v1–v8, quality_test_scene_v1) |
| Assets | `assets/golden_scene/` (299 MB library: buildings/, city_real/, …) — after migrating the single baseline-used file, see below |
| Evidence | `artifacts/golden_scene/` |
| CI workflows (15) | `full-battlefield-production-v1.yml`, `full-battlefield-production-v2.yml`, `full-battlefield-v2-lod-verify.yml`, `full-battlefield-v8-candidate.yml` – `v12-candidate.yml`, `full-battlefield-town-probe-a–d.yml`, `golden-scene-v1-verify.yml`, `quality-material-proof-v1-verify.yml`, `quality-test-scene-v1-verify.yml` |

## Asset migration performed before recycle

`assets/golden_scene/city_real/church_landmark.glb` — the only golden_scene file referenced by the sole baseline (`river_town_visual_slice.gd:618`, the distant church landmark) — was moved to `assets/visual_slice/landmarks/church_landmark.glb` and the reference updated. The baseline asset graph is now fully self-contained under `assets/visual_slice/`. Its stale `.import` was dropped and is regenerated by the editor import pass.

## Verification

1. Zero references from any retained scene/script to any recycled path (filename, class, preload and `res://assets/golden_scene` greps).
2. Post-recycle re-render of `RiverTownVisualSlice.tscn` under the baseline protocol must reproduce the baseline captures (performed after this commit).

## Restoration

Any recycled path can be restored with:
`git checkout recycle/2026-09-20/pre-visual-baseline-rationalization -- <path>`
